# BayanSynthTTS — Python dependencies
# Install: pip install -r requirements.txt
# Or:      pip install -e .   (uses pyproject.toml)

# ── Core inference ──────────────────────────────────────────────────────────
numpy>=1.24
torch>=2.0
torchaudio>=2.0
soundfile>=0.12
PyYAML>=6.0

# ── Arabic TTS engine ───────────────────────────────────────────────────────
peft>=0.7                  # LoRA injection
transformers>=4.40
huggingface_hub<1.0        # ⚠ pinned — do NOT upgrade to >=1.0
omegaconf>=2.3
hyperpyyaml

# ── Arabic diacritization (tashkeel) ───────────────────────────────────────
mishkal                    # Rule-based (always required, zero GPU overhead)
# tashkeel                 # Neural upgrade (optional, GPU recommended)
# pip install tashkeel

# ── Optional: audio format conversion ──────────────────────────────────────
# For mp3/m4a/ogg input, install ffmpeg:
#   Windows: winget install ffmpeg   OR   choco install ffmpeg
#   Linux:   sudo apt install ffmpeg
#   macOS:   brew install ffmpeg

# ── Optional: Gradio UI ─────────────────────────────────────────────────────
gradio>=4.0

# ── CosyVoice3 TTS engine + audio decoder ────────────────────────────────────
# Installs the cosyvoice package from the CosyVoice-Arabic fork.
# Required for standalone use outside the CosyVoice-Arabic repo.
cosyvoice @ git+https://github.com/Ramendan/CosyVoice-Arabic.git
# Matcha-TTS provides the HiFi-GAN audio decoder used by CosyVoice3.
matcha-tts
