# Minimal `matcha.models` package stubs used for local dry-run / UI startup.
# These are light stand-ins so the CosyVoice control UI can import model classes
# referenced in YAML without pulling the full Matcha-TTS dependency.
from .components import *
